logging:
  level:
    root: trace
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  threads:
    virtual:
      enabled: true
  datasource:
    generate-unique-name: false
    name: tacodb
  data:
    mongodb:
      database: tacodb
      username: taco
      password: taco
server:
  shutdown: graceful